# 🧪 RaceDay Database Validation

## Overview

The RaceDay database was created and tested using SQL Server Management Studio (SSMS).

The database was checked to confirm that the required tables and sample records were created successfully.

## Validation Checks

The following areas were verified:

- Database tables were created successfully.
- Primary keys were defined.
- Foreign key relationships were created.
- Required fields use appropriate constraints.
- User records were inserted successfully.
- Event records were inserted successfully.
- Event categories were inserted successfully.
- Sample participant enrolments were inserted successfully.

## Sample Data

The database includes:

- 2 Organisers
- 2 Participants
- 3 Events
- Categories for each event
- Sample participant enrolments

## Verification

SELECT queries were executed in SSMS to view the inserted records and confirm that the sample data was stored correctly.

The database validation was completed without SQL execution errors.

## Database Script

The complete SQL script is available in:

`RaceDay_Database.sql`
